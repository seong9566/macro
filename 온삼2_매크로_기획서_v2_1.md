# 온라인삼국지2 전용 매크로 기획서 (v2)

## 1. 문제 정의

| 항목 | 상태 |
|------|------|
| 이미지 인식 | ✅ 정상 (화면 캡처 기반이라 입력 방식과 무관) |
| 마우스 커서 이동 | ✅ 정상 (SetCursorPos 등 소프트웨어 이동 허용) |
| 마우스 클릭 | ❌ 차단 (소프트웨어 생성 클릭을 게임이 무시) |

**핵심 원인:** 온삼2 클라이언트가 마우스 클릭 이벤트의 `dwExtraInfo` 플래그 또는
입력 소스를 검사하여, 하드웨어가 아닌 소프트웨어에서 생성된 클릭을 필터링하고 있음.

---

## 2. 기술 스택



```
Python 3.10+
├── opencv-python    # 이미지 인식 (템플릿 매칭)
├── pyautogui        # 스크린 캡처, 마우스 이동
├── pydirectinput    # DirectInput 레벨 클릭 (1차 시도)
├── ctypes           # Win32 API 직접 호출 (2차 시도)
├── keyboard         # 키보드 입력 (마우스키 우회용)
├── pywin32          # 게임 창 핸들 감지 (win32gui)
└── time, random     # 딜레이, 랜덤화
```

---

## 3. 클릭 방식별 우선순위 (순서대로 시도)

### 🥇 1단계: pydirectinput (DirectInput 레벨)

일반 pyautogui보다 낮은 레벨에서 SendInput API를 사용.
많은 게임에서 이 방식을 허용함.

```python
import pydirectinput

pydirectinput.click(x, y)
```

### 🥈 2단계: ctypes SendInput (하드웨어 시뮬레이션 플래그)

SendInput API를 직접 호출하면서 dwExtraInfo를 0으로 설정.
소프트웨어 입력 표식을 제거하여 하드웨어처럼 보이게 함.

```python
import ctypes
from ctypes import wintypes

# SendInput 구조체 정의 후 직접 호출
# MOUSEEVENTF_LEFTDOWN | MOUSEEVENTF_LEFTUP
# dwExtraInfo = 0 (하드웨어 입력처럼 위장)
```

### 🥉 3단계: 마우스키 우회

윈도우 접근성 기능(마우스키)을 활성화한 뒤,
키보드 넘패드 5번 입력 → OS가 마우스 클릭으로 변환.
게임 입장에서는 OS 내부 변환이라 차단이 어려움.

```python
import keyboard

# 마우스키 활성화 상태에서
keyboard.press_and_release('num 5')  # = 마우스 좌클릭
```

### 🏅 4단계: Interception 드라이버 (최종 수단)

Interception 드라이버를 설치하면 커널 레벨에서 입력을 주입.
하드웨어 입력과 100% 동일하게 처리됨.

```python
# interception-python 라이브러리 사용
# 드라이버 설치 필요 (관리자 권한)
```

---

## 4. 프로그램 구조

```
macro_project/
├── main.py              # 메인 실행 파일
├── config.py            # 설정값 (딜레이, 좌표, 임계값 등)
├── window_manager.py    # 게임 창 감지 및 영역 관리
├── image_finder.py      # 화면 캡처 + 이미지 인식 (OpenCV)
├── clicker.py           # 클릭 모듈 (다중 방식 지원)
├── macro_engine.py      # 매크로 시퀀스 실행 엔진
├── logger.py            # 구조화된 로그 기록
├── images/              # 인식할 이미지 템플릿 폴더
│   ├── button_ok.png
│   ├── npc_shop.png
│   └── ...
└── requirements.txt
```

> **변경점 (v1 → v2):**
> - `screen_capture.py` 제거 → `image_finder.py`에 캡처 로직 통합 (역할 중복 해소)
> - `window_manager.py` 신규 추가 → 게임 창 핸들 자동 감지, 영역 좌표 제공
> - `logger.py` 설계 추가 → 디버깅 및 운영 로그 체계화

---

## 5. 핵심 모듈 설계

### 5-1. logger.py (로그 기록)

```python
import logging
import os
from datetime import datetime

def setup_logger(name="macro", log_dir="logs", level=logging.DEBUG):
    """
    구조화된 로거를 생성하여 콘솔 + 파일에 동시 출력.
    
    로그 레벨 가이드:
        DEBUG    - 이미지 매칭 점수, 좌표 계산 상세값
        INFO     - 매크로 시작/중지, 클릭 실행, 이미지 발견
        WARNING  - 이미지 미발견 (timeout), 클릭 방식 fallback
        ERROR    - 템플릿 로딩 실패, 캡처 실패, 예외 발생
        CRITICAL - 게임 창 미감지, 복구 불가능한 오류
    """
    logger = logging.getLogger(name)
    logger.setLevel(level)
    
    # 이미 핸들러가 있으면 중복 추가 방지
    if logger.handlers:
        return logger
    
    # 로그 포맷
    fmt = logging.Formatter(
        "[%(asctime)s] %(levelname)-8s %(message)s",
        datefmt="%H:%M:%S"
    )
    
    # 콘솔 핸들러
    console = logging.StreamHandler()
    console.setLevel(logging.INFO)
    console.setFormatter(fmt)
    logger.addHandler(console)
    
    # 파일 핸들러 (일자별 로그 파일)
    os.makedirs(log_dir, exist_ok=True)
    today = datetime.now().strftime("%Y-%m-%d")
    file_handler = logging.FileHandler(
        os.path.join(log_dir, f"macro_{today}.log"),
        encoding="utf-8"
    )
    file_handler.setLevel(logging.DEBUG)
    file_handler.setFormatter(fmt)
    logger.addHandler(file_handler)
    
    return logger


# 모듈 전역 로거 (다른 모듈에서 import하여 사용)
log = setup_logger()
```

### 5-2. window_manager.py (게임 창 감지)

```python
import ctypes
import ctypes.wintypes as wintypes
from logger import log

# ── win32gui 대신 ctypes로 직접 구현 (의존성 최소화) ──
user32 = ctypes.windll.user32

EnumWindows = user32.EnumWindows
GetWindowTextW = user32.GetWindowTextW
GetWindowTextLengthW = user32.GetWindowTextLengthW
IsWindowVisible = user32.IsWindowVisible
GetWindowRect = user32.GetWindowRect

WNDENUMPROC = ctypes.WINFUNCTYPE(
    ctypes.c_bool, wintypes.HWND, wintypes.LPARAM
)


def find_game_window(title_keyword):
    """
    창 제목에 keyword가 포함된 윈도우 핸들(HWND)을 반환.
    
    Args:
        title_keyword: 게임 창 제목에 포함된 문자열
    
    Returns:
        HWND(int) 또는 None
    """
    result = []
    
    def callback(hwnd, lparam):
        if IsWindowVisible(hwnd):
            length = GetWindowTextLengthW(hwnd)
            if length > 0:
                buf = ctypes.create_unicode_buffer(length + 1)
                GetWindowTextW(hwnd, buf, length + 1)
                if title_keyword in buf.value:
                    result.append(hwnd)
        return True
    
    EnumWindows(WNDENUMPROC(callback), 0)
    
    if result:
        log.info(f"게임 창 발견: HWND={result[0]}")
        return result[0]
    
    log.critical(f"게임 창 미발견: '{title_keyword}' 포함 윈도우 없음")
    return None


def get_window_region(hwnd):
    """
    윈도우 핸들로부터 (x, y, w, h) 영역을 반환.
    image_finder의 region 파라미터에 직접 전달 가능.
    
    Returns:
        (x, y, width, height) 또는 None
    """
    if hwnd is None:
        return None
    
    rect = wintypes.RECT()
    if GetWindowRect(hwnd, ctypes.byref(rect)):
        x = rect.left
        y = rect.top
        w = rect.right - rect.left
        h = rect.bottom - rect.top
        log.debug(f"게임 창 영역: x={x}, y={y}, w={w}, h={h}")
        return (x, y, w, h)
    
    log.error("GetWindowRect 실패")
    return None


def get_game_region(title_keyword):
    """
    게임 창 제목으로 검색 → 영역 좌표 반환 (편의 함수).
    
    Returns:
        (x, y, width, height) 또는 None
    """
    hwnd = find_game_window(title_keyword)
    return get_window_region(hwnd)
```

### 5-3. image_finder.py (화면 캡처 + 이미지 인식)

```python
import cv2
import numpy as np
import pyautogui
import time
import os
from logger import log

# ── 템플릿 캐시 (동일 이미지 반복 로딩 방지) ──
_template_cache = {}


def _load_template(template_path):
    """
    템플릿 이미지를 로딩하고 캐시에 저장.
    파일이 없거나 손상되면 예외를 발생시킴.
    """
    if template_path in _template_cache:
        return _template_cache[template_path]
    
    if not os.path.exists(template_path):
        log.error(f"템플릿 파일 없음: {template_path}")
        raise FileNotFoundError(f"템플릿 파일을 찾을 수 없습니다: {template_path}")
    
    template = cv2.imread(template_path)
    if template is None:
        log.error(f"템플릿 로딩 실패 (손상된 이미지?): {template_path}")
        raise ValueError(f"이미지를 읽을 수 없습니다: {template_path}")
    
    _template_cache[template_path] = template
    log.debug(f"템플릿 로딩 완료: {template_path} ({template.shape})")
    return template


def capture_screen(region=None):
    """
    화면을 캡처하여 OpenCV BGR 배열로 반환.
    
    Args:
        region: (x, y, w, h) 또는 None (전체 화면)
    
    Returns:
        numpy.ndarray (BGR) 또는 None (캡처 실패 시)
    """
    try:
        screenshot = pyautogui.screenshot(region=region)
        screen = cv2.cvtColor(np.array(screenshot), cv2.COLOR_RGB2BGR)
        return screen
    except Exception as e:
        log.error(f"화면 캡처 실패: {e}")
        return None


def find_image(template_path, confidence=0.8, region=None):
    """
    화면에서 템플릿 이미지를 찾아 중심 좌표를 반환.
    
    Args:
        template_path: 찾을 이미지 경로
        confidence: 매칭 임계값 (0.0 ~ 1.0)
        region: 탐색 영역 (x, y, w, h) - None이면 전체 화면
    
    Returns:
        (center_x, center_y) 또는 None
    
    Raises:
        FileNotFoundError: 템플릿 파일이 없을 때
        ValueError: 템플릿 이미지가 손상되었을 때
    """
    # 템플릿 로딩 (캐시 활용)
    template = _load_template(template_path)
    
    # 화면 캡처
    screen = capture_screen(region=region)
    if screen is None:
        return None
    
    # 템플릿이 화면보다 클 경우 방어
    if (template.shape[0] > screen.shape[0] or 
        template.shape[1] > screen.shape[1]):
        log.warning(
            f"템플릿이 탐색 영역보다 큼: "
            f"template={template.shape[:2]}, screen={screen.shape[:2]}"
        )
        return None
    
    # 매칭 수행
    result = cv2.matchTemplate(screen, template, cv2.TM_CCOEFF_NORMED)
    min_val, max_val, min_loc, max_loc = cv2.minMaxLoc(result)
    
    log.debug(
        f"매칭 결과: {template_path} → "
        f"score={max_val:.3f} (threshold={confidence})"
    )
    
    if max_val >= confidence:
        h, w = template.shape[:2]
        center_x = max_loc[0] + w // 2
        center_y = max_loc[1] + h // 2
        
        # region 오프셋 보정
        if region:
            center_x += region[0]
            center_y += region[1]
        
        log.info(
            f"이미지 발견: {os.path.basename(template_path)} "
            f"→ ({center_x}, {center_y}) [score={max_val:.3f}]"
        )
        return (center_x, center_y)
    
    return None


def wait_for_image(template_path, timeout=10, interval=0.5, **kwargs):
    """
    이미지가 나타날 때까지 대기.
    timeout 초 내에 못 찾으면 None 반환.
    """
    start = time.time()
    while time.time() - start < timeout:
        try:
            pos = find_image(template_path, **kwargs)
            if pos:
                return pos
        except (FileNotFoundError, ValueError):
            # 템플릿 자체 오류는 재시도 의미 없음 → 즉시 중단
            return None
        time.sleep(interval)
    
    log.warning(
        f"이미지 대기 timeout ({timeout}초): "
        f"{os.path.basename(template_path)}"
    )
    return None
```

### 5-4. clicker.py (다중 방식 클릭)

```python
import time
import random
import ctypes
from ctypes import wintypes
from logger import log

# ══════════════════════════════════════════════
# Win32 INPUT 구조체 (올바른 Union 구조)
# ══════════════════════════════════════════════

MOUSEEVENTF_MOVE       = 0x0001
MOUSEEVENTF_LEFTDOWN   = 0x0002
MOUSEEVENTF_LEFTUP     = 0x0004
MOUSEEVENTF_ABSOLUTE   = 0x8000

INPUT_MOUSE    = 0
INPUT_KEYBOARD = 1
INPUT_HARDWARE = 2


class MOUSEINPUT(ctypes.Structure):
    _fields_ = [
        ("dx",          wintypes.LONG),
        ("dy",          wintypes.LONG),
        ("mouseData",   wintypes.DWORD),
        ("dwFlags",     wintypes.DWORD),
        ("time",        wintypes.DWORD),
        ("dwExtraInfo", ctypes.c_size_t),   # ← 수정: ULONG_PTR (정수형)
    ]


class KEYBDINPUT(ctypes.Structure):
    _fields_ = [
        ("wVk",         wintypes.WORD),
        ("wScan",       wintypes.WORD),
        ("dwFlags",     wintypes.DWORD),
        ("time",        wintypes.DWORD),
        ("dwExtraInfo", ctypes.c_size_t),
    ]


class HARDWAREINPUT(ctypes.Structure):
    _fields_ = [
        ("uMsg",    wintypes.DWORD),
        ("wParamL", wintypes.WORD),
        ("wParamH", wintypes.WORD),
    ]


class _INPUT_UNION(ctypes.Union):
    """Win32 INPUT 구조체의 Union 부분 (mi / ki / hi)"""
    _fields_ = [
        ("mi", MOUSEINPUT),
        ("ki", KEYBDINPUT),
        ("hi", HARDWAREINPUT),
    ]


class INPUT(ctypes.Structure):
    """Win32 INPUT 구조체 — Union을 올바르게 포함."""
    _fields_ = [
        ("type", wintypes.DWORD),
        ("union", _INPUT_UNION),
    ]


def _send_mouse_input(flags, abs_x=0, abs_y=0):
    """SendInput으로 마우스 이벤트 1건 전송 (내부 헬퍼)."""
    inp = INPUT()
    inp.type = INPUT_MOUSE
    inp.union.mi.dx = abs_x
    inp.union.mi.dy = abs_y
    inp.union.mi.mouseData = 0
    inp.union.mi.dwFlags = flags
    inp.union.mi.time = 0
    inp.union.mi.dwExtraInfo = 0  # 하드웨어 입력처럼 0
    
    sent = ctypes.windll.user32.SendInput(
        1, ctypes.byref(inp), ctypes.sizeof(INPUT)
    )
    if sent != 1:
        log.warning(f"SendInput 실패: flags=0x{flags:04X}")


# ══════════════════════════════════════════════
# 클릭 방식별 구현
# ══════════════════════════════════════════════

# ── 방식 1: pydirectinput ──
def click_directinput(x, y):
    import pydirectinput
    pydirectinput.moveTo(x, y)
    time.sleep(0.05)
    pydirectinput.click()
    log.debug(f"[directinput] 클릭: ({x}, {y})")


# ── 방식 2: ctypes SendInput (하드웨어 위장) ──
def click_sendinput(x, y):
    """ctypes SendInput으로 하드웨어 수준 클릭."""
    screen_w = ctypes.windll.user32.GetSystemMetrics(0)
    screen_h = ctypes.windll.user32.GetSystemMetrics(1)
    abs_x = int(x * 65535 / screen_w)
    abs_y = int(y * 65535 / screen_h)
    
    # 이동
    _send_mouse_input(
        MOUSEEVENTF_MOVE | MOUSEEVENTF_ABSOLUTE,
        abs_x, abs_y
    )
    time.sleep(0.05)
    
    # 다운
    _send_mouse_input(
        MOUSEEVENTF_LEFTDOWN | MOUSEEVENTF_ABSOLUTE,
        abs_x, abs_y
    )
    time.sleep(random.uniform(0.03, 0.08))
    
    # 업
    _send_mouse_input(
        MOUSEEVENTF_LEFTUP | MOUSEEVENTF_ABSOLUTE,
        abs_x, abs_y
    )
    log.debug(f"[sendinput] 클릭: ({x}, {y}) → abs({abs_x}, {abs_y})")


# ── 방식 3: 마우스키 (넘패드 5) ──
def click_mousekeys(x, y):
    """마우스키 기능으로 클릭 (사전에 마우스키 활성화 필요)."""
    import pyautogui
    import keyboard as kb
    
    pyautogui.moveTo(x, y)
    time.sleep(0.05)
    kb.press_and_release('num 5')
    log.debug(f"[mousekeys] 클릭: ({x}, {y})")


# ══════════════════════════════════════════════
# 통합 클릭 인터페이스
# ══════════════════════════════════════════════

CLICK_METHODS = {
    "directinput": click_directinput,
    "sendinput":   click_sendinput,
    "mousekeys":   click_mousekeys,
}


def click(x, y, method="sendinput"):
    """
    설정된 방식으로 클릭 실행.
    
    Args:
        x, y: 클릭 좌표 (스크린 절대 좌표)
        method: "directinput" | "sendinput" | "mousekeys"
    
    Raises:
        ValueError: 지원하지 않는 method
    """
    if method not in CLICK_METHODS:
        raise ValueError(
            f"지원하지 않는 클릭 방식: '{method}'. "
            f"사용 가능: {list(CLICK_METHODS.keys())}"
        )
    
    # 사람처럼 보이게 약간의 랜덤 오프셋
    offset_x = random.randint(-2, 2)
    offset_y = random.randint(-2, 2)
    
    CLICK_METHODS[method](x + offset_x, y + offset_y)
```

### 5-5. macro_engine.py (매크로 실행 엔진)

```python
import time
import random
from image_finder import find_image, wait_for_image
from clicker import click
from config import CLICK_METHOD, DEFAULT_DELAY
from logger import log


class MacroEngine:
    def __init__(self, click_method=CLICK_METHOD):
        self.click_method = click_method
        self.running = False
    
    def find_and_click(self, image_path, confidence=0.8,
                       timeout=10, delay_after=0.5, region=None):
        """이미지를 찾아서 클릭하는 기본 동작."""
        pos = wait_for_image(
            image_path, timeout=timeout,
            confidence=confidence, region=region
        )
        if pos:
            click(pos[0], pos[1], method=self.click_method)
            time.sleep(delay_after + random.uniform(0, 0.3))
            return True
        
        log.warning(f"find_and_click 실패: {image_path}")
        return False
    
    def run_sequence(self, steps):
        """
        매크로 시퀀스 실행 — 루프를 스택 기반으로 평탄화.
        
        steps 예시:
        [
            {"action": "find_click", "image": "images/btn.png", "delay": 1.0},
            {"action": "wait", "duration": 2.0},
            {"action": "find_click", "image": "images/ok.png", "timeout": 5},
            {"action": "loop", "count": -1, "steps": [...]},
        ]
        
        loop 처리 방식:
            - 재귀 호출 대신 스택(리스트)을 사용하여 평탄화
            - count=-1 은 무한 반복 (self.running이 False가 될 때까지)
            - 중첩 루프도 안전하게 처리 (스택 오버플로우 없음)
        """
        self.running = True
        log.info("매크로 시퀀스 시작")
        
        # 스택: 각 항목은 (steps_list, current_index, remaining_count)
        # remaining_count: -1=무한, 0=이번 회차 끝나면 pop, >0=남은 반복
        stack = [(steps, 0, 1)]
        
        while self.running and stack:
            current_steps, idx, remaining = stack[-1]
            
            # 현재 스텝 리스트를 끝까지 실행했으면 반복 처리
            if idx >= len(current_steps):
                remaining -= 1
                if remaining == 0:
                    # 반복 횟수 소진 → 이 레벨 종료
                    stack.pop()
                    continue
                else:
                    # 아직 반복 남음 (remaining=-1이면 무한이므로 항상 여기)
                    stack[-1] = (current_steps, 0, remaining)
                    continue
            
            step = current_steps[idx]
            # 인덱스 전진
            stack[-1] = (current_steps, idx + 1, remaining)
            
            action = step.get("action")
            
            if action == "find_click":
                self.find_and_click(
                    step["image"],
                    confidence=step.get("confidence", 0.8),
                    timeout=step.get("timeout", 10),
                    delay_after=step.get("delay", DEFAULT_DELAY),
                    region=step.get("region"),
                )
            
            elif action == "wait":
                duration = step.get("duration", 1.0)
                log.debug(f"대기: {duration}초")
                time.sleep(duration)
            
            elif action == "loop":
                count = step.get("count", -1)
                inner_steps = step.get("steps", [])
                # 스택에 새 레벨 push
                stack.append((inner_steps, 0, count))
                log.info(
                    f"루프 진입: "
                    f"{'무한' if count == -1 else str(count) + '회'} 반복"
                )
            
            else:
                log.warning(f"알 수 없는 액션: {action}")
        
        log.info("매크로 시퀀스 종료")
    
    def stop(self):
        self.running = False
        log.info("매크로 중지 요청")
```

### 5-6. config.py (설정)

```python
# ══════════════════════════════════════════════
# 클릭 방식 설정
# ══════════════════════════════════════════════
# "directinput" | "sendinput" | "mousekeys"
CLICK_METHOD = "sendinput"

# ══════════════════════════════════════════════
# 이미지 인식 설정
# ══════════════════════════════════════════════
DEFAULT_CONFIDENCE = 0.8     # 매칭 임계값 (0.0 ~ 1.0)
SEARCH_INTERVAL = 0.5        # 이미지 탐색 주기 (초)

# ══════════════════════════════════════════════
# 딜레이 설정
# ══════════════════════════════════════════════
DEFAULT_DELAY = 0.5          # 클릭 후 기본 대기 시간
MIN_CLICK_INTERVAL = 0.1     # 최소 클릭 간격

# ══════════════════════════════════════════════
# 매크로 시작/종료 단축키
# ══════════════════════════════════════════════
START_KEY = "F5"
STOP_KEY = "F6"

# ══════════════════════════════════════════════
# 게임 창 설정
# ══════════════════════════════════════════════
GAME_WINDOW_TITLE = "온라인삼국지"

# 게임 창 영역 자동 감지 사용 여부
# True  → window_manager로 창 위치/크기 자동 획득
# False → MANUAL_REGION 값을 수동으로 지정
AUTO_DETECT_WINDOW = True

# AUTO_DETECT_WINDOW = False 일 때 사용할 수동 영역 (x, y, w, h)
MANUAL_REGION = None  # 예: (0, 0, 1920, 1080)

# ══════════════════════════════════════════════
# 로그 설정
# ══════════════════════════════════════════════
LOG_DIR = "logs"
LOG_LEVEL = "DEBUG"          # DEBUG | INFO | WARNING | ERROR
```

### 5-7. main.py (메인 실행)

```python
import keyboard
import threading
from macro_engine import MacroEngine
from window_manager import get_game_region
from config import (
    START_KEY, STOP_KEY, CLICK_METHOD,
    GAME_WINDOW_TITLE, AUTO_DETECT_WINDOW, MANUAL_REGION
)
from logger import log

engine = MacroEngine(click_method=CLICK_METHOD)


def get_region():
    """설정에 따라 게임 창 영역을 가져옴."""
    if AUTO_DETECT_WINDOW:
        region = get_game_region(GAME_WINDOW_TITLE)
        if region is None:
            log.critical(
                f"게임 창을 찾을 수 없습니다: '{GAME_WINDOW_TITLE}'. "
                f"게임이 실행 중인지 확인하세요."
            )
        return region
    return MANUAL_REGION


# ── 매크로 시퀀스 정의 ──
def build_macro(region):
    """게임 영역 정보를 포함한 매크로 시퀀스 생성."""
    return [
        {"action": "loop", "count": -1, "steps": [
            {"action": "find_click", "image": "images/monster.png",
             "confidence": 0.8, "timeout": 5, "delay": 1.0,
             "region": region},
            {"action": "find_click", "image": "images/attack.png",
             "confidence": 0.8, "timeout": 3, "delay": 0.5,
             "region": region},
            {"action": "wait", "duration": 2.0},
        ]},
    ]


def start_macro():
    region = get_region()
    if region is None and AUTO_DETECT_WINDOW:
        log.error("매크로 시작 불가: 게임 창 미감지")
        return
    
    macro = build_macro(region)
    log.info(f"매크로 시작! (방식: {CLICK_METHOD}, 영역: {region})")
    thread = threading.Thread(
        target=engine.run_sequence,
        args=(macro,),
        daemon=True
    )
    thread.start()


def stop_macro():
    engine.stop()
    log.info("매크로 중지!")


# 단축키 등록
keyboard.add_hotkey(START_KEY, start_macro)
keyboard.add_hotkey(STOP_KEY, stop_macro)

log.info(f"[대기중] {START_KEY}=시작 / {STOP_KEY}=중지")
keyboard.wait()  # 프로그램 유지
```

---

## 6. 사용법

### 설치

```bash
pip install opencv-python pyautogui pydirectinput keyboard
```

> **참고:** `pywin32`는 `window_manager.py`에서 ctypes로 직접 구현했으므로
> 별도 설치 불필요. 만약 `win32gui`를 선호하면 `pip install pywin32` 후 교체 가능.

### 이미지 템플릿 준비

1. 온삼2에서 클릭할 버튼/UI를 스크린샷
2. 해당 영역만 잘라서 `images/` 폴더에 저장
3. 파일명을 알기 쉽게 지정 (예: `btn_attack.png`)

> 💡 **팁:** 이미지를 너무 크게 자르면 배경 변화에 취약하고,
> 너무 작으면 오인식률이 높아짐. 버튼 영역 + 약간의 여백이 적당.

### 클릭 방식 테스트

`config.py`에서 `CLICK_METHOD`를 바꿔가며 테스트:

```
1차: "directinput"  → 안 되면
2차: "sendinput"    → 안 되면  
3차: "mousekeys"    → 안 되면 Interception 드라이버 고려
```

### 실행

```bash
# 관리자 권한 CMD에서 실행
python main.py
```

### 로그 확인

```bash
# 실시간 로그 확인 (별도 터미널)
type logs\macro_2025-03-21.log

# 또는 PowerShell로 실시간 추적
Get-Content logs\macro_*.log -Wait
```

---

## 7. 주의사항

- **반드시 관리자 권한**으로 Python / CMD를 실행해야 게임에 입력 전달 가능
- **게임은 창 모드**로 실행하는 것을 권장 (풀스크린은 입력 차단 가능성 높음)
- 클릭 간격에 **랜덤 딜레이**를 넣어 패턴 감지를 회피
- 이미지 템플릿은 **현재 해상도에서 캡처**해야 정확히 매칭됨
- 해상도나 게임 창 크기를 변경하면 템플릿을 다시 캡처해야 함
- `AUTO_DETECT_WINDOW = True` 시 게임을 먼저 실행한 뒤 매크로 시작
- 로그 파일이 누적되므로 `logs/` 폴더를 주기적으로 정리

---

## 8. v1 → v2 변경 요약

| 항목 | v1 | v2 |
|------|----|----|
| `dwExtraInfo` 타입 | `ctypes.POINTER(c_ulong)` (포인터) | `ctypes.c_size_t` (정수형, ULONG_PTR) |
| `INPUT` 구조체 | `MOUSEINPUT` 직접 포함 | Union으로 MI/KI/HI 올바르게 구분 |
| 루프 실행 | 재귀 호출 (`run_sequence` → `run_sequence`) | 스택 기반 평탄화 (스택 오버플로우 방지) |
| 에러 핸들링 | 없음 | 템플릿 로딩·캡처·매칭 전 구간 try-catch |
| `screen_capture.py` | 존재 (미구현, 역할 중복) | 제거 → `image_finder.py`에 통합 |
| `logger.py` | 폴더에만 존재 (미설계) | 콘솔+파일 듀얼 출력, 레벨별 가이드 정의 |
| 게임 창 감지 | 수동 설정만 가능 | `window_manager.py`로 자동 감지 + 수동 fallback |
| 이미지 region | 수동 지정 필수 | 게임 창 좌표 자동 연동 |
| 템플릿 캐시 | 없음 (매 탐색마다 imread) | `_template_cache` 딕셔너리로 1회만 로딩 |
